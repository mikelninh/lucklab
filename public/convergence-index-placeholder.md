# placeholder

The real Luck Convergence Index PDF should live at `public/luck-convergence-index.pdf`. Until then, the `/api/subscribe` route sends the user an HTML email with a link back to the Diagnostic.
